export default {
    typescript: {
        ignoreBuildErrors: true, // disable TypeScript checking on build
    },
    eslint: {
        ignoreDuringBuilds: true, // disable ESLint checking on build
    },
};